# SPA fallback. Netlify and Cloudflare Pages read this; Render does NOT — its
# static sites need the equivalent Rewrite rule set in the dashboard instead.
# Kept for portability, and because it is served as a plain file on Render it is
# deliberately short.
/*    /index.html   200
